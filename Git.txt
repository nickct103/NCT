# Git: Version control tool

# Github: A cloud for uploading project tracked using Git.
    -Alternatives:
        -Gitlab
        -Bitbucket

# Terms:
    -Repository: Project folder (being tracked by Git)
        -local repository: Local project folder
        -remote repository: Project folder on Github
    -stage: Prepare files to be committed
    -commit: Save changes
    -push: upload
    -pull: Download

#Local Workflow
        - 3 areas:
            -Working directory: This is where developer code (red in terminal)
            -staging area: changes ready to be 'commited' are moved here (green in terminal)
            -local repository (.git): committed changes are saved here.
        
# Steps to upload a project on Github
    - Github:
        -Create a new remote repository
            -name: Name of the project
            -description: Description of the project
            -Visibility: Private/Public
                -Public (open source)
            -README.md: Project description file
    -Local:
        First time:
            -git init: Initialize a local git repository
            -git add: Stage files
                -git add file1.html: Stage file`.html
                -git add .: Stage all files
            -git commit -m "Message": Commit changes
            -git remote add origin [remote repository link]: Link local repository to remote repository
            -git push -u origin main- Upload the code to the remote repository linked in 'master' branch
        -Second time and onwards
            -git add: Stage files
                -git add file1.html: Stage file`.html
                -git add .: Stage all files
            -git commit -m "Message": Commit changes
            -git push: Push commited changes to master branch in the remote repository

        -Generic
            -git status: Current status of local repository
    -Working with Remotes
# Steps to download a project on Github
    -First time:
        -git clone [remote repository link]: Clone of online repository in local machine
    -Second time and onwards
        -git pull: Pull latest commits from 'master' branch in the remote repository to local repository
# Branches:
    -Default (master/main)
    -Separate branches can be created to work on individual features. For ex., in Amazon: Separate branches for Amazon pay, Shopping, Streaming